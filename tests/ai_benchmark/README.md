# Atlas AI Benchmark

Suite reproducible y sin chain-of-thought para comparar `fast`, `reasoning`,
`deep` y el router automático. Las pruebas registran rol, fallback, latencia,
fuentes y herramientas mediante `AIExecutionResult`; los proveedores de las
pruebas son simulados y no realizan llamadas externas.


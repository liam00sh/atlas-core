# Proyecto Atlas - dependencias de Python
#
# Instalación:
#     python -m pip install -r requirements.txt
#
# Datos IANA de zonas horarias. Necesario en Windows para ZoneInfo
# (Europe/Madrid, UTC, cambios de horario de verano, etc.).
tzdata==2026.3

# Información de CPU, memoria, discos, procesos y sensores del sistema.
# Requerido por core/system_info.py y por los tests que importan Atlas/IA/tools.
psutil>=7.0,<8.0

# Carga de variables desde el archivo .env.
# Requerido por la monitorización de Raspberry y el supervisor.
python-dotenv>=1.0,<2.0

# Framework de pruebas utilizado por run_tests_safe.ps1.
pytest>=9.0,<10.0

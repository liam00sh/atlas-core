# Comandos

Cada archivo Python de esta carpeta define un comando independiente con sus metadatos, alias y función `execute()`.

`console/command_manager.py` descubre y carga automáticamente los comandos disponibles al iniciar Atlas.
